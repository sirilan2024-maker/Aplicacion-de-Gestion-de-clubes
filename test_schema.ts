import { createClient } from '@supabase/supabase-js'; require('dotenv').config({path: '.env.local'}); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false }, global: { fetch: (...args) => fetch(args[0], { ...args[1], dispatcher: new (require('undici').Agent)({ connect: { rejectUnauthorized: false } }) }) } }); async function test() { const { data, error } = await supabase.from('notifications').insert([{ title: 'test', content: 'test', match_id: '123' }]); console.log(error); } test();
