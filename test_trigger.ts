import { createClient } from '@supabase/supabase-js'; require('dotenv').config({path: '.env.local'}); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false }, global: { fetch: (...args) => fetch(args[0], { ...args[1], dispatcher: new (require('undici').Agent)({ connect: { rejectUnauthorized: false } }) }) } }); async function test() { const { data, error } = await supabase.rpc('get_triggers', {}); console.log('This will fail if rpc is not there'); } test();
