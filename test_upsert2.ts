import { createClient } from '@supabase/supabase-js'; require('dotenv').config({path: '.env.local'}); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false }, global: { fetch: (...args) => fetch(args[0], { ...args[1], dispatcher: new (require('undici').Agent)({ connect: { rejectUnauthorized: false } }) }) } }); async function test() { const { data, error } = await supabase.from('attendance').upsert([{ session_id: '9b979323-d7cf-4287-9a9a-f442dfe9f0ca', event_id: '9b979323-d7cf-4287-9a9a-f442dfe9f0ca', player_id: '123e4567-e89b-12d3-a456-426614174000', status: 'Presente', date: '2026-08-02' }], { onConflict: 'session_id,player_id' }); console.log('Upsert:', error); } test();
